g++ nota.cpp -o nota //compilar
./nota// ejecutar
 rm -R trabajo2.cpp //Elimina Archivos
touch hola.cpp // crear archivos, es importante ponerle extencion.

////Python
comandos basicos

Python --version
Esto permite saber la version de python en el ordenador

Python es un programa en la cual su sintaxis es fuertemente tipado.

para ejecutar un programa en python deberas hacerlo con la siguiente sintaxis

Python y LC001.py

Libreria para automatizacion

para abrir una carpeta completa con archivo en visual code estudios es
code . //luego precionas enter

pip install pyautogui
pip install tk
pip install SpeechRecognition
pip install pyttsx3
pip install pywhatkit
pip install wikipedia
pip install pygame
pip install keyboard
pip install opencv-python
pip install numpy
pip install Selenium

//Serial Port
pip install pyserial
python.exe -m pip install --upgrade pip

//py audio
se debe instalar el archivo descargado
pip install PyAudio-0.2.11-cp38-cp38-win_amd64.whl
//Se encuentra en la carpeta python en Py_Audio

//Otro metodo no utilizado
pip install pipwin
pipwin install pyaudio

Python(PIP)
Lenguaje de programacion interpretado, fuertemente tipado, su funcionalidad era que los niños se interesen en los
lenguajes emergente de programacion usando su creatividad.
pero el lenguaje fue tan sencillo que las comunidades de software lo acuñaron como el mejor lenguaje interpretado y facil de usar.
asi que los ingeniero y desarroladores de software empesaron a enriqueser el codigo añadiendole con cada año nuevas funcionalidades.

JavaScript(FrontEnd) NODEJS(BackEnd)(NPM)(NPX)
Es un lenguane interpretado, ya que no necesita compilarse, asi que su sintaxis es parecida al de c++ con python

NodeJS(BackEnd)
Node correo bajo el lado del servidor, y consta de paquetes de trabajo o librerias que enriquesen su funcionamiento.
Las paqueterias a trabajar son:

(NPM)
npm init -y // crear rapido
npm install johnny-five //instala la libreria
node index.js // ejecuta el programa

////////////////////
si tienes los pakages.json
npm install


/////metodos
myLed.stop()
myLed.on()
myLed.off()
myLed.Strobe(2000)
myLed.blink(1000)

///Variables de entorno
alias crear="touch"
alias borrar="rm -R"
sintaxis:
crear app.js //Crear el archivo
borra app.js //Borrar el archivo

////Dormir
sleep 5 && crear app.js


(NPX)



Ruby(GEM) y las Gemas
Ruby es un lenguaje insterpretado muy parecido a python, por lo de fuertemente tipado, es un lenguaje interpretado
pero es mas ordenado. y su sintaxis de programacion es mas facil de usar.

///Comando de SSH
para ingresar a otra computadora de linux, deberas hacer una conexcion remota eso se logra
reconociendo la ip y el nombre de usuario de la maquina

Sintaxis: ssh inori@192.168.1.168
Luego pones la contraseña configurada en el servidor